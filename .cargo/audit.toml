[advisories]
# Transitive via broker SDKs; no fixed upgrade available upstream (matches the
# same ignore already used in higgs/deny.toml elsewhere in the fleet).
# h2 0.3 / rustls-webpki 0.101: aws-smithy-http-client → hyper 0.14 / rustls 0.21
# until the AWS SDK stack moves off those lines.
ignore = [
    "RUSTSEC-2023-0071",
    "RUSTSEC-2026-0258",
    "RUSTSEC-2026-0098",
    "RUSTSEC-2026-0099",
    "RUSTSEC-2026-0104",
]
